
/*
 * main loop
 */
    void main_loop() {
    servo_initialize();
    
    servo_set_duty(SERVO_X, 1562);
    servo_set_duty(SERVO_Y, 1546);
    
    lcd_printf("Calibration...");
    
    while(TRUE) {
    }
}